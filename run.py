from market import app

if __name__=='__main__':
    app.run(debug = True)#debug made true for no time to setup some environment variables(lazy)
    #above debug thing is the same thing we did in terminal for flask now we r doing here
    